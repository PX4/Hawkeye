<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Material 3 base. Activity is fullscreen OpenGL via NativeActivity, so the
         only surfaces this theme styles in practice are the prototype dialogs. -->
    <style name="Theme.HawkeyeAndroid" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>
</resources>
